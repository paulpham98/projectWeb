{% extends 'base.html.twig' %}

{% block body %}
 <div class="container-md col-md-8 text-center">
 {# {% if book != null %} #}
     <table class="table table-striped|sm|bordered|hover|inverse table-inverse table-responsive">
      <thead class="thead-inverse|thead-default">
          <tr>
              <th>Category id</th>
            <th>Category Image</th>
              <th>Category Name</th>

              <th>Category Code</th>
              <th> Category Description</th>
              {# <th>Course Detail</th> #}

              
              
          </tr>
          </thead>
          <tbody>
               <tr>
                  <td>{{ category.id }}</td>
                  <td>
                      <img src="{{ asset('covers/'~category.image) }}" width="100" height="120">  
                  </td>
                  <td>{{ category.name }} $</td>
                  <td>{{ category.code }}</td>
                   <td>{{ category.description }}</td>

                  {# <td>
                    {% if (category.courses != null)  %}
                       <a class="text-decoration-none" href="{{ path('genre_detail', {'id' : book.genre.id }) }}">
                          {{ ca.courses.name }}
                        </a> 
                    {% endif %}
                  </td> #}
                  <td> 
                    {# {% if (category.courses != null) %}
                         {% for courses in category.courses %}
                          <a class="text-decoration-none" href="{{ path('courseDetail_detail', {'id' : courses.id }) }}">
                          {{ courses.name }}
                          </a>
                          <br>
                      {% endfor %}
                    {% endif %} #}
                  </td>
                  {# <td>
                    <a class="btn btn-warning" href="{{ path('book_edit', {'id' : book.id })}}">
                    Edit
                    </a>
                    <a class="btn btn-danger" href="{{ path('book_delete', {'id' : book.id }) }}"
                    onclick = "return confirm('Do you want to delete this book ?')"
                    >
                    Delete
                    </a>
                  </td> #}
              </tr>          
          </tbody>
    </table>
  
</div>
{% endblock %}